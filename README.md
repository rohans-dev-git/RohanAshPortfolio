<div align="center">
</div>


## Run Locally

**Prerequisites:**  Node.js
1. Install dependencies:
   `npm install`
2. Setting the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Running the app:
   `npm run dev`
